/**
 * Algorithms UI stub.
 *
 * IMPORTANT: If webpack still reports errors in this file at lines 30+,
 * discard the unsaved editor tab for AlgorithmsPopup.tsx (it is an old
 * full version conflicting with the restored 88e2dab API).
 */
export const AlgorithmsToolButton = () => {
  return null;
};

export const AlgorithmsControls = () => {
  return null;
};

/** @deprecated */
export const AlgorithmsPopup = AlgorithmsToolButton;
